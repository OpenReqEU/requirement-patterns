/**
 * This package is used to contain all the domain classes with they
 * corresponding annotation of JPA
 */
@GenericGenerator(name = "customGenerator", strategy = "edu.upc.gessi.rptool.data.mediators.IdGenerator")
package edu.upc.gessi.rptool.domain;

import org.hibernate.annotations.GenericGenerator;
